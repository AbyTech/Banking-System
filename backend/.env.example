# --- Previous .env.example content ---
# # Django
# SECRET_KEY=your-super-secret-key-here
# DEBUG=True
# ALLOWED_HOSTS=localhost,127.0.0.1,.railway.app,.vercel.app
# 
# # Database
# DATABASE_URL=postgres://primewave:primewave@localhost:5432/primewave_bank
# 
# # Redis
# REDIS_URL=redis://localhost:6379
# 
# # Email (SendGrid)
# SENDGRID_API_KEY=your-sendgrid-api-key
# DEFAULT_FROM_EMAIL=noreply@primewavebank.com
# 
# # JWT
# JWT_SECRET=your-jwt-secret-key
# 
# # CORS
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# 
# # Stripe (optional)
# STRIPE_SECRET_KEY=your-stripe-secret-key
# STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key

# --- Revised .env.example for clarity ---
# This file is an example. For local development, copy it to a file named .env
# and fill in the values. For production, use your hosting provider's
# environment variable settings.

# Django Core Settings
# IMPORTANT: Change this in production!
SECRET_KEY=your-super-secret-key-here

# DEBUG should be False in production
DEBUG=True

# A comma-separated list of allowed hosts.
# For production on Render, you should set this to your Render URL,
# e.g., your-app-name.onrender.com
# The settings.py file reads the 'ALLOWED_HOSTS' variable.
ALLOWED_HOSTS=localhost,127.0.0.1

# Database URL
# For local development with PostgreSQL.
# For production on Render, this is usually set automatically if you link a database.
DATABASE_URL=postgres://user:password@localhost:5432/mydatabase

# Redis URL
# Used for caching and Celery.
REDIS_URL=redis://localhost:6379

# Email settings (using SendGrid as an example)
SENDGRID_API_KEY=your-sendgrid-api-key
DEFAULT_FROM_EMAIL=noreply@example.com

# JWT Secret Key
JWT_SECRET=your-jwt-secret-key

# CORS settings
# Comma-separated list of origins allowed to make cross-site requests.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# CSRF Trusted Origins
# Comma-separated list of trusted origins for CSRF.
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Stripe (optional)
STRIPE_SECRET_KEY=your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
